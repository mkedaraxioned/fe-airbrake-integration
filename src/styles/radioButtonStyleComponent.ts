import type { ComponentStyleConfig } from '@chakra-ui/theme';

const Radio: ComponentStyleConfig = {
  sizes: {},
  variants: {
    primary: {
      colorScheme: 'btnPurple',
    },
  },
  defaultProps: {},
};

export default Radio;
